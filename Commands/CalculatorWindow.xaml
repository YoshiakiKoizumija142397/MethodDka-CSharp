<Window x:Class="MethodDkaWPFApp.Views.CalculatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MethodDkaWPFApp.Views"
        mc:Ignorable="d"
        Title="MethodDka Calculator" Height="450" Width="600">

	<Grid Margin="20">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<!-- 次数入力 -->
		<StackPanel Orientation="Horizontal" Margin="0 0 0 10">
			<TextBlock Text="次数 n：" VerticalAlignment="Center" Width="80"/>
			<TextBox Text="{Binding Degree}" Width="100"/>
		</StackPanel>

		<!-- 係数入力 -->
		<StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 0 0 10">
			<TextBlock Text="係数：" VerticalAlignment="Center" Width="80"/>
			<TextBox Text="{Binding Coefficients}" Width="400"/>
		</StackPanel>

		<!-- 結果表示 -->
		<StackPanel Grid.Row="2">
			<Button Content="計算" Width="100" Height="30"
                    HorizontalAlignment="Left"
                    Command="{Binding SolveCommand}"/>

			<ListBox ItemsSource="{Binding Results}" Margin="0 10 0 0"/>
		</StackPanel>
	</Grid>
</Window>
